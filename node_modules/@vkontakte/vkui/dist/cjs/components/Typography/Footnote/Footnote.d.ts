import * as React from 'react';
import { HasCaps, TypographyProps } from '../Typography';
export interface FootnoteProps extends TypographyProps, HasCaps {
}
/**
 * @see https://vkcom.github.io/VKUI/#/Footnote
 */
export declare const Footnote: ({ className, caps, Component, normalize, ...restProps }: FootnoteProps) => React.JSX.Element;
